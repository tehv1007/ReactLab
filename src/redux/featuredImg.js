import { HOME_IMAGE } from '../shared/images';

export const HomeImage = (state = HOME_IMAGE, action) => {
        switch(action.type) {
            default:
                return state;
        };
};